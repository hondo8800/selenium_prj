Sample project for selenium

Use "**pip install -r requirements.txt**" in terminal to install pytest and selenium

Use "**pytest -s -v [options]**" to run tests

  [options]:  --browser chrome
              --browser edge

